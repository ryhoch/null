# Contracts

Placeholder for future smart contracts (DEX, on-chain transaction features).

Planned stack: Foundry (forge, cast, anvil) for EVM contracts.

## Setup (future)

```bash
curl -L https://foundry.paradigm.xyz | bash
foundryup
forge init
```
